# 개발·배포 문서

**이 문서는 프로그램을 넘겨주는 쪽이 읽습니다.**
받는 쪽이 읽는 것은 [README.md](README.md) 이고, 거기에는 소스나 빌드
이야기가 일부러 하나도 없습니다.

---

## 목차

- [1. 소스 파일 구성](#1-소스-파일-구성)
- [2. 소스로 돌리기](#2-소스로-돌리기)
- [3. exe 로 묶어 배포하기](#3-exe-로-묶어-배포하기)
  - [3-1. 만들기](#3-1-만들기)
  - [3-2. exe 하나에 역할 셋](#3-2-exe-하나에-역할-셋)
  - [3-3. 작업 스케줄러는 그대로 둡니다](#3-3-작업-스케줄러는-그대로-둡니다)
  - [3-4. 설정 파일을 번들에 넣지 않는 이유](#3-4-설정-파일을-번들에-넣지-않는-이유)
  - [3-5. 경로 처리](#3-5-경로-처리)
  - [3-6. 걸릴 만한 것](#3-6-걸릴-만한-것)
- [4. 넘기기 전 점검](#4-넘기기-전-점검)

---

## 1. 소스 파일 구성

**프로그램 본체**

| 파일 | 역할 |
|---|---|
| `수집기.py` | 매일 돌리는 본체 |
| `점수.py` | 점수표를 읽어 공고명을 채점 |
| `편집기.py` | 브라우저 설정 편집기 |
| `앱경로.py` | 설정·결과가 어느 폴더에 있는지 정하는 곳 ([3-5](#3-5-경로-처리)) |
| `점수표_검증.py` | 점수표 회귀 테스트. 검증용 공고 8건을 파일 안에 갖고 있음 |
| `점수표.yaml` | 공고명 판정 키워드 |
| `config.example.yaml` | `config.yaml` 견본. 번들에 들어가 첫 실행 때 꺼내짐 |

**소스로 돌릴 때 쓰는 것**

| 파일 | 역할 |
|---|---|
| `실행.bat` | 파이썬으로 수집기를 돌림 |
| `편집기.bat` | 파이썬으로 편집기를 띄움 |

**exe 로 묶을 때만 쓰는 것**

| 파일 | 역할 |
|---|---|
| `빌드.bat` | 더블클릭하면 배포판을 만듦 |
| `나라장터수집기.spec` | PyInstaller 설정 |
| `실행진입.py` | exe 진입점. 첫 인자로 역할을 가름 ([3-2](#3-2-exe-하나에-역할-셋)) |
| `배포\실행.bat` | 배포판에 들어갈 `실행.bat`. exe 를 부름 |

**개발용 (배포하지 않음)**

`진단.py` · `실측.py` · `업계참여추적.py` 는 점수표를 다듬을 때 쓰는
분석 스크립트입니다. 배포판에 들어가지 않고 `spec` 에도 없습니다.

의존 관계는 `수집기.py` → `점수.py` → `점수표.yaml` 이고,
셋 다 `앱경로.py` 를 씁니다. 하나라도 빠지면 실행되지 않습니다.

---

## 2. 소스로 돌리기

```
pip install requests pyyaml openpyxl pypdf pywin32
```

| 패키지 | 없으면 |
|---|---|
| `requests` `pyyaml` `openpyxl` | 아예 실행되지 않습니다 |
| `pypdf` | 공고문 PDF 검사(3·4차)를 건너뜁니다 |
| `pywin32` | 아웃룩으로 메일을 못 보냅니다 (`mail.mode: outlook` 일 때만 필요) |

```
python 편집기.py          설정 편집기
python 수집기.py          config.yaml 의 hours 만큼
python 수집기.py 30       최근 30일
python 점수표_검증.py     점수표 회귀 테스트
python 점수.py "대체투자자산 공정가치평가 용역" "국민연금공단"
```

`실행.bat` · `편집기.bat` 의 `set "PY=..."` 는 이 PC 의 파이썬 경로입니다.
다른 PC 에서 소스로 돌린다면 `where python` 으로 확인해 고쳐야 합니다.
**배포판에는 이 줄이 없습니다.**

> `.bat` 파일은 **CP949(ANSI) + CRLF** 로 저장해야 합니다. UTF-8 로 저장하면
> cmd 가 배치 파일을 읽는 위치를 잃어 명령이 앞부분부터 잘린 채 실행됩니다
> (`python 수집기.py` → `thon 수집기.py`). `.gitattributes` 의 `*.bat -text`
> 가 git 쪽 정규화를 막아 두었습니다. 편집기로 열어 저장할 때만 조심하세요.

---

## 3. exe 로 묶어 배포하기

담당자 PC 에 파이썬을 깔기 어렵거나, 깔더라도 `pip install` 과
`실행.bat` 의 파이썬 경로 수정까지 시키기 어려울 때 씁니다.
PyInstaller 로 파이썬과 패키지를 폴더 하나에 담아 넘깁니다.

### 3-1. 만들기

소스가 있는 PC 에서 **`빌드.bat` 을 더블클릭**하면 끝입니다.
처음에는 1~2분 걸립니다.

```
dist\나라장터 수집기\      ← 이 폴더를 통째로 복사해서 넘깁니다
```

`빌드.bat` 이 하는 일은 네 가지입니다.

1. `pyinstaller` 와 패키지를 설치·갱신
2. `나라장터수집기.spec` 대로 exe 를 묶음
3. `배포\실행.bat` 과 `README.pdf`(→ `설명서.pdf`) 를 배포 폴더에 넣음
4. **`build\` 를 지움** — 아래 설명

**`build\` 를 지우는 이유.** 그 안에도 `나라장터수집기.exe` 가 있습니다.
COLLECT 전 단계의 껍데기라 옆에 `_internal` 이 없고, 실행하면 부트로더가
`python314.dll` 을 못 찾아 **검은 창만 깜빡이고 죽습니다.** dist 의 진짜
exe 와 이름이 글자 하나 다르지 않아서, 빌드가 끝난 폴더를 열어본 사람이
build 쪽을 먼저 누를 여지가 큽니다. 실제로 그렇게 헷갈린 적이 있습니다.
다음 빌드가 20초쯤 더 걸리지만 그편이 낫습니다.

명령으로 하려면 이렇게도 됩니다. 이때는 `build\` 가 남으니 직접 지우세요.

```
pip install pyinstaller
pyinstaller 나라장터수집기.spec --noconfirm
```

**`README.pdf` 를 먼저 갱신하세요.** `빌드.bat` 은 있는 파일을 그대로
`설명서.pdf` 로 복사할 뿐입니다. `README.md` 를 고쳤다면 PDF 를 다시 뽑고
빌드해야 배포판의 설명서가 최신이 됩니다.

### 3-2. exe 하나에 역할 셋

실행 파일은 하나뿐이고 **첫 인자로 역할을 나눕니다** (`실행진입.py`).

| 명령 | 하는 일 | 누가 부르나 |
|---|---|---|
| `나라장터수집기.exe` | 설정 편집기를 브라우저에 엶 | 담당자가 더블클릭 |
| `나라장터수집기.exe 수집` | 공고 수집 | `실행.bat` → 작업 스케줄러 |
| `나라장터수집기.exe 점수표검증` | 점수표 회귀 검사 | 편집기의 검증 버튼 |

exe 를 여러 개 만들어 서로 찾게 하면 경로가 어긋날 때마다 깨집니다.
하나로 두면 편집기가 수집기를 자식으로 띄울 때도 `sys.executable`(= 자기
자신)을 그대로 쓰면 되므로 찾을 일이 없습니다. `편집기.py` 의 `child()`
가 그 부분입니다.

**콘솔을 남깁니다.** 편집기를 띄운 창이 곧 '닫는 방법'(Ctrl+C)이고,
창이 없으면 표준출력이 사라져 자식 프로세스 로그 수집이 꼬입니다.

**출력 인코딩을 코드에서 고정합니다.** 묶으면 `PYTHONUTF8` 환경변수가
먹지 않아 출력이 파이프나 파일로 갈 때 cp949 가 됩니다. `—` 한 글자에
`UnicodeEncodeError` 로 죽습니다. 편집기의 실행 탭과 `실행.bat` 의 `2>`
리다이렉트가 둘 다 그 경로라 실제로 터졌습니다.
`실행진입.py` 의 `_utf8_출력()` 이 stdout·stderr 을 UTF-8 로 고정합니다.

**`창_붙잡기()`** 는 편집기 모드와 잘못된 명령에서만 Enter 를 기다립니다.
`수집` 에는 붙이지 않습니다. 작업 스케줄러 뒤에서 입력을 기다리면 작업이
'실행 중' 인 채로 남아 다음 회차까지 막습니다. 멈춰 세울지는 `실행.bat` 이
`auto` 인자를 보고 판단합니다.

### 3-3. 작업 스케줄러는 그대로 둡니다

스케줄러에 등록된 것은 `실행.bat` 의 **경로**뿐입니다. 배포판의 `실행.bat`
은 파일 이름과 `auto` 인자를 그대로 유지한 채 안에서 exe 를 부르므로,
**같은 폴더에 덮어쓴다면 스케줄러는 손댈 필요가 없습니다.**

다만 **폴더 경로가 바뀌면 다시 등록해야 합니다.** `/tr` 에 절대경로가
박혀 있기 때문입니다.

배포판 `실행.bat` 에는 `set "PY=..."` 두 줄이 없습니다.
대신 `"%~dp0나라장터수집기.exe" 수집` 을 부릅니다. 나머지 줄은 같습니다.

### 3-4. 설정 파일을 번들에 넣지 않는 이유

`config.yaml` 과 `점수표.yaml` 은 담당자가 편집기로 고치는 파일입니다.
exe 안에 넣으면 다시 묶기 전까지 못 고칩니다. 그래서 번들에는
**기본값만** 넣어 두고, 첫 실행 때 exe 옆으로 한 번 꺼냅니다
(`앱경로.py` 의 `seed()`). 이미 있으면 손대지 않으므로 새 버전을 덮어써도
고쳐 둔 설정이 남습니다.

인증키가 든 실제 `config.yaml` 은 번들에 들어가지 않습니다.
꺼내지는 것은 `config.example.yaml` 이고 인증키 칸은 비어 있습니다.
이미 쓰던 `config.yaml` 이 있다면 exe 옆에 같이 넣어 주면 됩니다.

담당자가 파일을 망가뜨렸을 때 **지우고 다시 실행하면 기본값이 새로
생기는** 것도 이 구조 덕입니다.

### 3-5. 경로 처리

묶으면 `__file__` 이 `_internal` 안을 가리킵니다. 그런데 `config.yaml` ·
`수집결과` · `로그` 는 exe 옆에 있어야 합니다. 둘을 섞으면 "저장했는데
다음 실행 때 그대로다" 같은, 원인을 찾기 어려운 증상이 납니다.

`앱경로.py` 가 두 폴더를 이름부터 갈라 놓습니다.

| | 소스로 돌릴 때 | exe 로 묶었을 때 |
|---|---|---|
| `app_dir()` | 소스 폴더 | **exe 가 놓인 폴더** |
| `bundle_dir()` | 소스 폴더 | `_internal`(`sys._MEIPASS`) |

`수집기.py` · `점수.py` · `편집기.py` · `점수표_검증.py` 가 전부
`app_dir()` 을 씁니다. 새 모듈을 추가할 때도 `Path(__file__).parent` 대신
`app_dir()` 을 쓰세요.

### 3-6. 걸릴 만한 것

| 증상 | 원인 / 대처 |
|---|---|
| 백신이 exe 를 지우거나 격리 | 서명 없는 PyInstaller exe 는 오탐이 잦습니다. 사내 IT 에 예외 등록을 요청하세요. 막히면 임베디드 파이썬 배포로 돌려야 합니다 |
| 검은 창만 깜빡이고 죽음 | `_internal` 이 없는 exe 를 실행한 것입니다. `build\` 쪽을 눌렀거나 폴더가 깨졌습니다 |
| 설정을 저장했는데 반영이 안 됨 | `_internal` 안의 `config.yaml` 을 고친 경우입니다. exe **옆**의 것을 고쳐야 합니다 |
| 빌드는 됐는데 실행 시 `ModuleNotFoundError` | 새로 쓴 패키지를 `나라장터수집기.spec` 의 `hiddenimports` 에 넣으세요 |
| 한글이 `?` 로 깨져 나옴 | 출력이 파이프로 갈 때입니다. `_utf8_출력()` 이 빠지지 않았는지 확인 |
| 배포 폴더가 너무 큼 | 28MB 정도가 정상입니다. `spec` 의 `excludes` 에 안 쓰는 패키지를 더 넣으면 줄어듭니다 |

`upx` 압축은 일부러 껐습니다. 용량은 줄지만 백신 오탐이 눈에 띄게 늘어납니다.
`onefile` 도 쓰지 않습니다. 실행할 때마다 임시 폴더에 수십 MB 를 풀어야 해서
시작이 느리고, 역시 백신이 더 자주 잡습니다.

---

## 4. 넘기기 전 점검

배포 폴더에서 아래를 확인하고 넘기세요. 인증키 없이도 앞의 세 개는 됩니다.

1. **`나라장터수집기.exe` 더블클릭** → 검은 창이 남아 있고 브라우저에
   편집기가 열리는가. `config.yaml` · `점수표.yaml` 이 옆에 새로 생겼는가
2. **`나라장터수집기.exe 점수표검증`** → `전부 통과`, 종료코드 0
3. **`실행.bat auto`** → 인증키가 비어 있으면 안내 메시지와 종료코드 1.
   `로그\실행.log` 가 UTF-8 로 남았는가
4. 실제 인증키를 넣고 **편집기 실행 탭에서 `--메일없이` 로 한 번** →
   엑셀·공고문 PDF 가 생기고 종료코드 0

**넘기기 전에 `config.yaml` 을 지우세요.** 시험하느라 실제 인증키를 넣었다면
그대로 딸려 갑니다. `수집결과` · `로그` · `백업` 도 같이 지워 처음 받은
상태로 되돌립니다. (`빌드.bat` 을 다시 돌리면 `dist\` 를 통째로 다시 만들기
때문에 저절로 깨끗해집니다.)

> **⚠ 시험할 때 메일이 실제로 나갑니다.**
> `실행.bat` 에는 `--메일없이` 가 없습니다. `config.yaml` 에 실제 수신자가
> 들어 있는 채로 돌리면 그대로 발송됩니다. 게다가 새 폴더는 수집 이력이
> 비어 있어 **지난 `hours` 기간치가 전부 '신규'** 로 나갑니다.
> 메일까지 확인해야 한다면 `mail.to` 를 본인 주소로 바꾸거나
> `draft_only: true` 로 두고 하세요.
